# Routines index

Routines (scheduled triggers) live in Riley's Claude account, not in git. Each `.json`
here is a backup with a `restore` block that maps onto a `create_trigger` call — see
[README.md](./README.md) to restore one.

The table below is generated by `scripts/format_routines.py` from `list_triggers`
output. Everything outside the AUTO markers is hand-written and survives regeneration.

<!-- BEGIN AUTO -->
_Table generated by `scripts/format_routines.py`. Last snapshot: 2026-09-12._

3 routine(s) backed up. Each `.json` carries a `restore` block that maps
onto a `create_trigger` call — see [README.md](./README.md) to restore one.

| Routine | Schedule | Enabled | File |
|---|---|---|---|
| Weekly Chess.com Review | `0 12 * * 1` (UTC) | yes | [`chess-weekly-review.json`](./chess-weekly-review.json) |
| First Light Brief — hourly | `0 0-5,13-23 * * *` (UTC) | yes | [`first-light-brief-hourly.json`](./first-light-brief-hourly.json) |
| Weekly Task Review | `0 1 * * 1` (UTC) | yes | [`weekly-task-review.json`](./weekly-task-review.json) |

<!-- END AUTO -->

## Companion state files

These are **not** routine backups — they have no `restore` block, and the script neither
writes nor prunes them. Each holds the current URL of an Artifact its routine writes to,
so the link stays stable instead of drifting to a new URL each run:

- [`chess-weekly-review-artifact.json`](./chess-weekly-review-artifact.json) — read by
  *Weekly Chess.com Review*, which republishes the whole page every week.
- [`first-light-brief-artifact.json`](./first-light-brief-artifact.json) — for
  *First Light Brief*, which writes into the artifact's database rather than
  republishing the page. That artifact is installed as an app on Riley's laptop and
  phone, so its URL must not change.

## Documentation

- [`first-light-brief.md`](./first-light-brief.md) — how the hourly briefing dashboard
  works end to end: install steps, the artifact db schema, the Google Tasks write-back
  contract, and known issues.

## Retired

`retired/` holds backups of routines that are no longer on the account. The script moves
them there rather than deleting them, so a restore block is never lost to a routine
being turned off.

- [`weekly-rollup-dashboard-refresh.json`](./weekly-rollup-dashboard-refresh.json) — no
  longer on the account; last seen before the 2026-09-02 drift (see the note below). It
  will move to `retired/` on the next regeneration. Restore it from its `restore` block
  first if it was turned off by accident.

## Notes

- 2026-09-13 — "First Light Brief" renamed **Throughline**. Same artifact and URL. Living doc is now `routines/throughline.md`; `routines/first-light-brief.md` is superseded.
- 2026-09-13 — Scheduled tasks are now `Throughline — hourly` (trig_01UGimbrAf3miYHyVDQQv744) and `Throughline — weekly review` (trig_015hXDZZKqzKJRieMbGs57dq, Sundays 21:00 Pacific). The old `first-light-brief-*.json` backups retire on the next backup run.

**2026-09-12 — script fixed.** `format_routines.py` had drifted from what `list_triggers`
returns and was extracting an empty prompt from every routine, which is why the chess
routine had to be backed up by hand. Three fixes:

- It now reads the prompt from `derived_state.prompt`, falling back to
  `session_request.events[].payload.internal_anthropic_catchall.message.content`, with
  the old `job_config.ccr` path kept as a last resort.
- It no longer deletes files it does not own. The previous version pruned every `*.json`
  not matching a live routine — which would have deleted both companion state files
  above and broken the stable-URL mechanism they exist for.
- Backup filenames are now keyed to the trigger id, so renaming a routine updates its
  file in place instead of orphaning it and creating a second one.

Regenerating now reproduces the hand-written chess backup byte-for-byte apart from two
added informational fields, which is the check that the drift is actually closed.

> **Drift, 2026-09-02.** `list_triggers` returned **zero** routines on the account, and this
> file had not been regenerated since 23 August, so the control plane briefly versioned one dead
> routine and no live ones.
>
> Between 29 August and 2 September, two self-re-arming PR check-in routines fired 50+ times — each run
> creating a fresh one-shot about two hours out, watching draft PRs that could only change by hand. Both
> were deleted on 2 September along with the PRs they watched.
>
> When rebuilding any check-in routine, prefer a fixed cron or an event-driven Monitor over
> "re-arm silently if nothing changed" — that pattern has no stopping condition when the
> thing it watches only changes by hand.
