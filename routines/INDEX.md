# Routines index

_Auto-generated by `scripts/format_routines.py`. Last snapshot: 2026-08-23. Hand-annotated 2026-09-02 and 2026-09-12._

2 routine(s) backed up with a `restore` block; 2 more are live on the account but not
yet backed up (see the gap note below). Each `.json` backup maps onto a `create_trigger`
call — see [README.md](./README.md) to restore one.

| Routine | Schedule | Enabled | Backed up | File |
|---|---|---|---|---|
| Weekly rollup dashboard refresh | `0 14 * * 1-5` (UTC) | yes | yes | [`weekly-rollup-dashboard-refresh.json`](./weekly-rollup-dashboard-refresh.json) |
| Weekly Chess.com Review | `0 12 * * 1` (UTC) | yes | yes | [`chess-weekly-review.json`](./chess-weekly-review.json) |
| First Light Brief — hourly | `0 0-5,13-23 * * *` (UTC) | yes | **no** | [`first-light-brief.md`](./first-light-brief.md) (docs only) |
| Weekly Task Review | Sundays | yes | **no** | — |

## Companion state files

These are **not** routine backups. Each holds the current URL of an Artifact its routine
writes to, so the link stays stable instead of drifting to a new URL each run:

- [`chess-weekly-review-artifact.json`](./chess-weekly-review-artifact.json) — read by
  *Weekly Chess.com Review*, which republishes the whole page every week.
- [`first-light-brief-artifact.json`](./first-light-brief-artifact.json) — for
  *First Light Brief*, which writes into the artifact's database rather than
  republishing the page. The artifact is installed as an app on Riley's laptop and
  phone, so this URL must not change.

## Backup gaps

**2026-09-12.** Two live routines have no `restore` block here:

- **First Light Brief — hourly** (`trig_01UGimbrAf3miYHyVDQQv744`) — an hourly briefing
  dashboard feeding an Artifact, with Google Tasks write-back. Fully documented in
  [`first-light-brief.md`](./first-light-brief.md), but the prompt itself lives only on
  the account.
- **Weekly Task Review** (`trig_01UAJhcpWSchwSqvq4TxZi8k`, Sundays) — found by a live
  account check on 2026-09-12.

Back both up the next time `backup-claude-setup` runs. *Weekly Chess.com Review* was
hand-backed-up on 2026-09-12 rather than run through `format_routines.py`, because the
script's shape no longer matches what `list_triggers` returns — see the drift note.

> **Drift, 2026-09-02.** `list_triggers` now returns **zero** routines on the account. The routine backed
> up above no longer exists there, and this file has not been regenerated since 23 August, so the control
> plane currently versions one dead routine and no live ones.
>
> Between 29 August and 2 September, two self-re-arming PR check-in routines fired 50+ times — each run
> creating a fresh one-shot about two hours out, watching draft PRs that could only change by hand. Both
> were deleted on 2 September along with the PRs they watched.
>
> Two follow-ups: re-run `backup-claude-setup` to regenerate this file against the live account, and
> decide whether *Weekly rollup dashboard refresh* should be restored from its `restore` block or was
> deliberately retired. When rebuilding any check-in routine, prefer a fixed cron or an event-driven
> Monitor over "re-arm silently if nothing changed" — that pattern has no stopping condition when the
> thing it watches only changes by hand.
