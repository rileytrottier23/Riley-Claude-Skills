# Routines index

_Auto-generated by `scripts/format_routines.py`. Last snapshot: 2026-08-23. Hand-annotated 2026-09-02._

1 routine(s) backed up. Each `.json` file carries a `restore` block that maps
onto a `create_trigger` call — see [README.md](./README.md) to restore one.

| Routine | Schedule | Enabled | File |
|---|---|---|---|
| Weekly rollup dashboard refresh | `0 14 * * 1-5` (UTC) | yes | [`weekly-rollup-dashboard-refresh.json`](./weekly-rollup-dashboard-refresh.json) |

> **Drift, 2026-09-02.** `list_triggers` now returns **zero** routines on the account. The routine backed
> up above no longer exists there, and this file has not been regenerated since 23 August, so the control
> plane currently versions one dead routine and no live ones.
>
> Between 29 August and 2 September, two self-re-arming PR check-in routines fired 50+ times — each run
> creating a fresh one-shot about two hours out, watching draft PRs that could only change by hand. Both
> were deleted on 2 September along with the PRs they watched.
>
> Two follow-ups: re-run `backup-claude-setup` to regenerate this file against the live account, and
> decide whether *Weekly rollup dashboard refresh* should be restored from its `restore` block or was
> deliberately retired. When rebuilding any check-in routine, prefer a fixed cron or an event-driven
> Monitor over "re-arm silently if nothing changed" — that pattern has no stopping condition when the
> thing it watches only changes by hand.
